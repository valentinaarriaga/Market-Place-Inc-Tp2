apiVersion: v1
kind: Service
metadata:
  name: orders-service
spec:
  type: NodePort
  selector:
    app: orders-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30007